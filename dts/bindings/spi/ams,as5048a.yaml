description: AMS AS5048A 14-bit magnetic rotary position sensor (SPI)

compatible: "ams,as5048a"

include: spi-device.yaml

properties:
  int-gpios:
    type: phandle-array
    description: Interrupt/alert output GPIO, active low.
